#!/bin/bash

PATH=$PATH:~/bin/support

theme
if [[ "$1" = "-r" ]]; then
  exit
fi

restart-emacs
term --class dropdown &
dunst >/tmp/dunst.log 2>&1 &
redshift &
bar &
drp start -i &
startlocker &
spotify &
xrandr --auto &
i3-msg workspace "1: I"
