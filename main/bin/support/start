#!/bin/bash

PATH=$PATH:~/bin/support

if [[ "$1" = "-r" ]]; then
  retheme
  exit
fi

retheme startup # Blocks

restart-emacs
term --class dropdown -e /bin/bash &
dunst >/tmp/dunst.log 2>&1 &
redshift &
bar &
drp start -i &
startlocker &
spotify &
xrandr --auto &
i3-msg workspace "1: I"
